---
- name: validate windows server
  hosts: windows
  gather_facts: false
  become: true

  tasks:
    - name: Ensure WinRM is enabled
      ansible.windows.win_shell: echo true
      register: result
      changed_when: false
